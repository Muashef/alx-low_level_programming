#include <stdio.h>

/**
 * main - function
 * @argc: code
 * @argv: code
 * Return: Always 0
 */

int __attribute__((unused)) main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
